# Cue

! command -v cue &> /dev/null || eval $(cue completion zsh)

# Dagger

! command -v dagger &> /dev/null || eval $(dagger completion zsh)

# Kube

! command -v kubectl &> /dev/null || eval $(kubectl completion zsh)

# Rclone

! command -v rclone &> /dev/null || eval $(rclone completion zsh)
